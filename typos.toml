[files]
# Cargo.lock holds checksums; the changelog is generated from commit subjects
# and is corrected at the source rather than after generation.
extend-exclude = ["CHANGELOG.md", "Cargo.lock", "*.snap", "tests/visual/reference/*"]
